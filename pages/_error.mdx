// pages/_error.js
import React from "react";

// Remove getInitialProps and use getStaticProps instead
export async function getStaticProps() {
  return {
    props: {}, // You can add custom props here if needed
  };
}

// _error component
function Error() {
  return (
    <div>
      <h1>Error occured</h1>
      <p>This is a custom error page.</p>
    </div>
  );
}

export default Error;
